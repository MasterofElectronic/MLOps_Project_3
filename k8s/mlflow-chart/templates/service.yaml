apiVersion: v1
kind: Service
metadata:
  name: mlflow-svc
  
spec:
  selector:
    app: mlflow
  ports:
    - name: tracking
      port: 5000
      targetPort: 5000
      nodePort: 30500
  type: NodePort
