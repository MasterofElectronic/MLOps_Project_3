apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: mlops
data:
  datasources.yaml: |-
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        access: proxy
        url: http://prometheus-svc.mlops.svc.cluster.local:9090
        isDefault: true
      - name: PostgreSQL
        type: postgres
        uid: ffllx8gs4w0sge
        access: proxy
        url: postgres-svc.ingesta.svc.cluster.local:5432
        user: mlops
        database: mlops
        jsonData:
          sslmode: 'disable'
          postgresVersion: 15
        secureJsonData:
          password: mlops2026
